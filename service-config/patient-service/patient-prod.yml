#Define the service running port
server:
  port: 9191

#Create database for particular service
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dbPatient?createDatabaseIfNotExist=true
    username: root
    password: 
    driver-class-name: com.mysql.cj.jdbc.Driver
